version: 2

updates:
  # Keeps the SHA-pinned actions in .github/workflows up to date.
  # Without this, pinning means the pins quietly go stale.
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    commit-message:
      prefix: "ci"
